CI
on:
  push,pull_request
  banch  main
jobs:
  build
    runs-on : ubuntu-latest
    steps
      - name : code checkout
        uses : actions/checkout@v2
      -name : java install
         uses : actions/java@v2
